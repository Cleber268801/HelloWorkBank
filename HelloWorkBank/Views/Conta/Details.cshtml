@model HelloWorkBank.Model.ContaModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ContaModel</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomeConta)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeConta)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.IdConta">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
